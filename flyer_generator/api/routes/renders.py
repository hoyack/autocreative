"""Render artifact streaming routes. Populated in Plan 20-11."""

from __future__ import annotations

from fastapi import APIRouter

router = APIRouter(tags=["renders"])

# Endpoints filled by Plan 20-11.
