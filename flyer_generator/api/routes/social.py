"""Social post + campaign routes. Populated in Plan 20-10."""

from __future__ import annotations

from fastapi import APIRouter

router = APIRouter(tags=["social"])

# Endpoints filled by Plan 20-10.
